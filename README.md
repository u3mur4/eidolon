# Eidolon

**Eidolon** (/aɪˈdoʊlən/) is Greek for a "phantom" or "double"—a proxy that stands in for a real binary to observe its behavior.

Eidolon is a transparent command-line interceptor. It executes any command as a proxy, siphoning its I/O (stdin/stdout/stderr) and metadata to a central log server for analysis without the host application knowing.
